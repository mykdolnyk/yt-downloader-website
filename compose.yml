services:
  web:
    build: .
    command: sh -c "python ytviewer/manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports: 
      - "8000:8000"

  nginx:
    image: nginx:latest
    ports:
      -"80:8001"